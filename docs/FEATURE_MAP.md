# Feature Map

Critical workflows, where their code/data actually live, and what a change
to them can ripple into. Built for impact analysis before a change — "if I
touch this, what else might break?" — not as full API documentation. Verify
against current code before trusting a specific file path; this can drift.

---

## Core features

| Feature | UI entry | Main logic | Data | External dependency | Critical? |
|---|---|---|---|---|---|
| AR Reminder (cycle tracking & reminders) | `app/billing/page.tsx` (AR tab), `app/ar-reminder/page.tsx` | `app/api/ar-reminder/{generate,sync-workflow,search,latest,history,export}` | `ar_reminder` | TeamWork (AGM/AR event scrape via `lib/teamwork-agm.ts`) | Yes — drives client reminders and billing eligibility |
| EOT (Extension of Time) | `app/master-list/eot/page.tsx`, `components/EotTable.tsx` | `app/api/ar-reminder/eot` (read); detection lives inside `late-filing/sync`'s own loop | `ar_reminder` (`ar_/agm_original_due_date`, `ar_/agm_revised_due_date` columns) | TeamWork (struck-through due-date HTML, `parseLatestDmy`) | Yes — wrong parsing here directly causes false OVERDUE flags |
| Late Filing detection | `app/late-filing/page.tsx` | `app/api/late-filing/sync` | `late_filing_companies`, mirrors into `ar_reminder` | TeamWork | Yes |
| Master List (Active Client, Ad-hoc, MAS, Name Change, Strike Off, Terminated, Trademark) | `app/master-list/*` | `app/api/master-list`, `app/api/master-list/move` | `master_list`, `companies` | TeamWork company sync (`app/api/teamwork/sync`) | Yes — visibility for compliance/billing status across the whole client base |
| Nominee Directors (ND) | `app/nominee-directors/page.tsx` | `app/api/nominee-directors`, `app/api/nominee-directors/subrole-remark`, scrape via `app/api/teamwork/sync-nd` (4-batch daily cron) | `nominee_directors`, `nd_appointments`, `automation_exceptions` (`missing_nominee_subrole`) | TeamWork scrape (Playwright, `lib/teamwork-nd.ts`) | Yes |
| Companies / Active Client roster | `app/companies/page.tsx` | `app/api/companies`, `.../parent`, `.../service-override` | `companies` | TeamWork sync (`app/api/teamwork/sync`, `lib/teamwork-company-profile.ts`) | Yes — source of truth PIC/active-status/UEN most other features join against |
| Company 360 (per-company aggregate view) | `app/companies/[id]/page.tsx` | `lib/company-360.ts` (`getCompany360`, shared by the page and `app/api/companies/[id]/route.ts`) | Reads across `companies`, `master_list`, `ar_reminder`, `generated_invoices`, `quickbooks_invoices`, `nd_appointments`, `email_drafts`, `post_incorporate_operations`, `trademark_records`, `teamwork_company_officials`, `teamwork_shareholder_shares` (added 2026-09-03 — officials/paid-up-capital, matched by exact UEN, not fuzzy); `companies.ssic_code_1/2` etc. (also added 2026-09-03, synced by `teamwork/sync-secretary` — see INV-TW-016/017); its Outstanding section (added 2026-09-08) calls `lib/soa-data.ts`'s `computeSoaRows()` directly (same computation the on-screen Outstanding/SOA pages use, with an optional `customerNamePrefilter` to scope it to one company instead of every customer) — see this file's own module comment for which links are real FKs vs. fuzzy `company_name` matches | none beyond the DB itself | Medium — a read-only aggregate view; a wrong fuzzy match shows a `matchScore`/`matchedVia` flag rather than failing silently. A bug in `computeSoaRows()` now surfaces here too, not just on the Outstanding pages |
| My Tasks (per-staff task aggregation + chat, 2026-09-08 redesign) | `app/my-tasks/page.tsx` | `app/api/my-tasks`; core computation extracted to `lib/my-tasks-data.ts`'s `computeMyTasks()`, shared with `app/api/assistant`'s `my_tasks_summary` tool. Same day, redesigned around a persistent ChatGPT-style chat interface (Vincent: "My Tasks 不要这样的，看起来好像有用，实际没有用处，只是摆设" then "可以New Chat, 记录Chats and tasks, 可以 Pin/ Pinned") — the static task tables + daily "Today's Priority" banner (`lib/my-tasks-brief.ts`'s `generateMyTasksBrief()`) moved to a secondary "Tasks" sidebar tab, chat (`lib/ai-conversations.ts`, `app/api/ai/conversations/*`) is now the primary view, backed by the same `/api/assistant` engine. Same day (later): 3rd sidebar tab "Activity", `lib/recent-activity.ts`'s `getRecentActivity()` — a real audit-trail timeline (who actually generated an invoice / edited AR / created a campaign / edited Master List / sent a client email / generated Post Incorporate docs / edited a Trademark record / set an SOA owner), prompted by Vincent catching the View-As-Chelsea screen showing 0 tasks despite her real, heavy daily use — the AR/Late-Filing lens above simply couldn't see any of it | `ar_reminder`, `late_filing_companies` (via `mirrored_ar_reminder_id`); `ai_conversations`/`ai_messages` (new, `scripts/add-ai-conversations.sql` — not yet run, see `docs/CURRENT_STATE.md`); the Activity tab reads `generated_invoices`/`ar_reminder`/`email_campaigns`/`master_list`/`email_drafts`/`post_incorporate_operations`/`trademark_records`/`soa_owners`' own pre-existing `created_by_email`/`updated_by_email`/`sent_by_email` audit columns (no new table) | Anthropic API (briefing + chat — both degrade gracefully without a key or before migrations run) | Yes for the 6 AR-Reminder-restricted accounts, whose only other page is AR Reminder itself — a bug here is their primary way to know what's overdue. Chat threads are ownership-checked on every read/write in `app/api/ai/conversations/**` (literal owner OR `canViewAsOthers` — see `docs/INVARIANTS.md` INV-DATA-018) — since 2026-09-08 the Tasks tab's "View As" picker ALSO drives chat: a privileged account can fully operate as the target, including starting a new chat that's created under the target's own email and becomes part of their real history, not just previewing their task data |
| Reports (customer-profile analytics, 2026-09-03; interactive "Explore" rebuild same day) | `app/reports/page.tsx` | `app/api/reports`, `app/api/reports/export`, `lib/reports-data.ts` (the one companies+master_list join, shared by both routes) | `companies`, `master_list` (`list_type`/`join_date`/`update_date`, exact-UEN joined — not fuzzy), `quickbooks_invoices`, `ar_reminder` | none beyond the DB itself | Low — gated to 4 named accounts (`ApprovedAccount.canViewReports`), enforced on BOTH `/api/reports` and `/api/reports/export` (the export route deliberately does NOT copy `app/api/export/company-data/route.ts`'s missing auth check). Read-only except `companies.customer_source` (edited from Company 360). The Explore section groups/filters the full active-company dataset (`companyRows`) entirely client-side — no per-filter network round-trip. SSIC (`companies.ssic_description_1`) is now real, backfilled for the whole roster (`scripts/backfill-ssic-full-roster.js`). Per-company revenue is deliberately NOT offered as an Explore metric — see `notes.revenue` in the API response for why (the fuzzy company-name matching `docs/INVARIANTS.md`/this file's own "high-risk shared logic" table already flags has never run as a full-roster batch, and a false match here would land in a leadership-facing aggregate). The client-type chart is legal entity structure (`company_type`), not industry — don't conflate with SSIC. |
| Billing & QuickBooks invoicing | `app/billing/page.tsx` | `app/api/billing/*`, `app/api/quickbooks/*` | `generated_invoices`, `invoice_creation_reservations`, QuickBooks Online (TAB + TAC company files) | QuickBooks Online API | Yes — real money, real invoices |
| Client Communications | `app/client-communications/{campaigns,templates,drafts,history}` | `app/api/client-communications/*` | `email_drafts` + campaign tables | TeamWork (recipient/contact sync, `lib/teamwork-recipients.ts` + `lib/teamwork-contact-report.ts`); Draft Helper (separate desktop app, `lib/draft-helper-client.ts`) for the real Outlook send | Yes — real outbound client email |
| Post Incorporate document generation | `app/post-incorporate/page.tsx` | `app/api/post-incorporate/{generate,enrich,parse-bizfile}` | `post_incorporate_operations` | Bizfile PDF parsing (`lib/bizfile-parse.ts`) | Medium — only 1 of 13 planned document types is live (see `docs/CURRENT_STATE.md`) |
| Trademark tracking | `app/master-list/trademark/*`, `components/TrademarkTable.tsx` | `app/api/trademark` | `master_list` (trademark rows) | — | Medium |
| Automation Health dashboard | `app/page.tsx` (home) | `app/api/automation/health` | `automation_sync_runs`, `automation_exceptions` | — (reads the other crons' own audit trail) | Yes — the early-warning system for every other automation in this table; if this breaks, every other row's failures go silent |
| Assistant (AI chat widget + My Tasks' own full-page chat) | `components/AssistantWidget.tsx`, `app/my-tasks/page.tsx` (2026-09-08, shares this same backend rather than a second chat engine) | `app/api/assistant` | reads across most tables above; also `lib/my-tasks-data.ts`, `lib/activity-data.ts`, `lib/user-memories.ts` and `lib/ai-conversations.ts` since 2026-09-08 (`my_tasks_summary` / `my_activity_pattern` / `remember_this` tools, plus optional per-turn persistence via `conversationId`) | Anthropic API | Medium. Was fully anonymous before 2026-09-08 — now also resolves the real caller via `getRequestAccount(req)` so it can answer personal "what should I do today" / "what are my habits" questions and save memories; `account` is `null` when that lookup fails and every personal path handles that explicitly (never guesses whose tasks/habits/memories to show). `remember_this` only fires on an explicit user ask (see `docs/INVARIANTS.md` INV-DATA-017) — never auto-inferred from conversation tone or activity data. Since 2026-09-08, `my_tasks_summary` also accepts an optional target person (resolved via `lib/approved-accounts.ts`'s `findMentionedAccount`, e.g. "如果我是HC，我要做什么今天？") gated on the caller's own `canViewAsOthers`; separately, the whole route accepts a `viewAs` body field (`resolveViewAsAccount`) that substitutes the EFFECTIVE account for the entire request when the caller is privileged — see INV-DATA-018 for why these are two distinct mechanisms, not one. Same day, also gained `recent_activity_summary` (`lib/recent-activity.ts`, same `person`/permission pattern as `my_tasks_summary`) — real audit-trail history ("what has this person actually DONE"), distinct from `my_activity_pattern`'s page-view tracking. Also gained `preview_invoice_draft` (`lib/billing-lookup.ts` + `lib/billing-draft.ts`) — READ-ONLY Billing Drafts preview for one company, gated by the same `isWithinRestriction()` check the Billing Drafts page itself uses (not just `canViewAsOthers`-style logic); step 1 of an agentic-invoicing direction, no write path exists yet. 2026-09-09: the "preview tool + real card/modal + write through the exact existing validated endpoint" pattern (never a new parallel write path, the AI itself holds no write tool) shipped for 3 more real actions — `preview_invoice_edit` (`lib/invoice-edit-lookup.ts`, editing an already-generated QuickBooks invoice → `PATCH /api/quickbooks/update-invoice`), `preview_late_filing_resolve` (`lib/late-filing-lookup.ts` → `PATCH /api/late-filing`), and `preview_post_incorporate` (validates a caller-assembled `PostIncorporateInput` via `lib/docx-post-incorporate.ts`'s real `validatePostIncorporateInput()` → `POST /api/post-incorporate/generate`, which returns a binary ZIP, not JSON — the one card of the four needing blob/`createObjectURL` download handling instead of `res.json()`). Post Incorporate is a genuine guided-intake conversation, not a one-shot preview: Claude collects company/director/shareholder details a few at a time across multiple turns and is instructed (in `staticSystemPrompt()`) to never invent identity data (ID numbers, addresses, DOB, share details) — see `docs/INVARIANTS.md` for the invariant this establishes. `claudeAnswer()`'s history window widened from `messages.slice(-8)` to `-24` to keep that multi-turn intake from losing earlier-collected details |
| Activity Insights (real behavioral analytics, 2026-09-08; narrowed to Vincent-only + moved under Admin nav 2026-09-09) | `app/activity-insights/page.tsx`, nested as a level-2 item under the Admin nav group (`components/Sidebar.tsx`'s `ADMIN_NODE`, alongside Appearance Settings / AI Learning) — no longer its own level-1 nav item | `app/api/activity/log` (write sink, called from `components/AppShell.tsx` on every route change plus a handful of key-action call sites — see `lib/activity-client.ts`'s own callers), `app/api/activity/insights` (read), `lib/activity-data.ts` (the one read/write layer both routes and the assistant's `my_activity_pattern` tool all share) | `user_activity_events` (new table, `scripts/add-user-activity-events.sql` — starts empty at deploy, never backfillable, see `docs/INVARIANTS.md` INV-DATA-016) | none beyond the DB itself | Low — read-only analytics. The page/route gate moved from `ApprovedAccount.canViewActivityInsights` (4 named accounts) to `admin` (Vincent only) 2026-09-09, per Vincent: "这个放在 Admin 内的 2级标题...并且只有Vincent 可以看到". `canViewActivityInsights` itself is untouched (still true for Cindy/Samuell/Yee Soon) since it also gates the unrelated AI Learning candidates cross-staff view (`app/api/ai-learning/candidates`, `app/ai-learning/page.tsx`'s bulk-approve staff list) — only this feature's own enforcement changed. Every account can still see its OWN pattern via the assistant regardless of either flag (self-only, same reasoning as `my_tasks_summary`). The write sink (`/api/activity/log`) deliberately swallows every failure (bad session, malformed body, DB error) rather than surfacing one — a tracking call must never interrupt or fail the real action it's attached to |

---

## High-risk shared logic

A bug in any of these fans out across many features at once — treat a
change here with the widest blast-radius assumption, not the narrowest.

| Module | Used by | Risk if broken |
|---|---|---|
| `lib/teamwork-agm.ts` (`parseLatestDmy`, event scraping) | AR Reminder generate/sync-workflow, Late Filing sync, EOT | Wrong due dates / wrong "is this cycle still open" across the whole client base |
| `lib/teamwork-pic.ts` (`resolveTeamworkPic`) | AR Reminder, Master List, Active Client, Billing | Companies show raw TeamWork ids instead of staff names, or a co-assigned company loses one PIC |
| `lib/automation-sync.ts` (`withAutomationRun`, `AutomationRun`, `replaceAutomationExceptions`) | Every cron route (all rows in the Core Features table with a "TeamWork"/QuickBooks external dependency) | A bug here can silently break the Automation Health dashboard for *everything at once*, or wrongly auto-resolve/never-resolve exceptions |
| `lib/campaign-recipients.ts` | Every outbound email flow (Client Communications, Draft Helper hand-off) | Wrong To/CC on a real client email — see `docs/INVARIANTS.md` INV-MAIL-* |
| `lib/company-name.ts` (`normalize`, `matchScore`, `findUniqueBestMatch`) | Fuzzy company-name matching wherever a TeamWork `company_name` string must match a `companies` row (ND page, Master List reconciliation, Company 360's fuzzy sections, others) | A change here can shift which rows match across many features simultaneously, not just one |
| `lib/staff-directory.ts` (`findStaffEmails`, and since 2026-09-08 `staffMentionCandidates`) | Client communication CC resolution (`lib/client-comms-resolve.ts`) AND, since 2026-08-31, My Tasks' entire "is this row mine" logic (`app/api/my-tasks/route.ts`); since 2026-09-08 also feeds `lib/approved-accounts.ts`'s `findMentionedAccount` (assistant chat cross-person lookup) | Previously only miscounted a CC list; now a bug here also means a staff member sees someone else's tasks, or misses their own (see the real Samuell Ng email-mismatch bug this exact risk produced, `docs/INVARIANTS.md`), or the chat assistant resolves "如果我是X" to the wrong person entirely |
| `lib/approved-accounts.ts` (`resolveViewAsAccount`, `findMentionedAccount`) | My Tasks' Tasks tab AND chat sidebar/thread/composer (`app/my-tasks/page.tsx`), `app/api/ai/conversations/**`, `app/api/assistant` — added 2026-09-08 (`docs/INVARIANTS.md` INV-DATA-018) | The permission gate for "management can act as or ask about another real staff member." A bug here either lets an unprivileged account see/act as someone else (real privacy breach — Vincent's own private chat history, another person's task list) or silently misattributes a management action to the wrong target account |
| `lib/docx-xml.ts` (OOXML engine) | Every document-generation workflow (Post Incorporate now; the 12 planned future ones will all depend on this too) | A bug here breaks document generation project-wide, not per-workflow — has its own 46-check regression suite (`test-docx-xml.ts`), run it before changing this file |
| `lib/quickbooks.ts`, `lib/qb-invoice-conventions.ts` | Billing, TAB/TAC dual-company logic, invoice PDF/create/update routes | Wrong invoice amounts, wrong DocNumbers, or writes that don't match what QuickBooks itself shows — real financial risk |
| `app/api/billing/renewals/route.ts`'s `computeAllCompanyBilling()` (added 2026-09-08, extracted from what used to be inline in `GET()`) + `lib/billing-draft.ts`'s `computeDraftLines()` (ported from `app/billing/page.tsx`'s `initialLines`) | Billing Drafts page (via `GET`) AND the assistant chat's `preview_invoice_draft` tool (`lib/billing-lookup.ts`) | A bug here now affects what BOTH the real Billing Drafts UI and the chat preview show — they must never quietly diverge. `app/billing/page.tsx` still has its OWN un-migrated copy of the `initialLines` logic (not yet retrofitted to import from `lib/billing-draft.ts`) — a rule change made in one must be mirrored in the other by hand until that retrofit happens |
| `app/api/late-filing/route.ts`'s `getLateFilingList()` (added 2026-09-09, same extraction pattern as `computeAllCompanyBilling()` above) | Late Filing page (via `GET`) AND the assistant chat's `preview_late_filing_resolve` tool (`lib/late-filing-lookup.ts`) | Same risk shape as the billing row above — a bug here affects both the real page's list and the chat preview. `app/late-filing/page.tsx`'s own `resolve()` remarks logic (the `"Review:"→"Resolved:"` rule) is separately ported (not extracted) into `lib/late-filing-lookup.ts` — a change to that specific rule must be mirrored by hand in both places |
| `lib/quickbooks-invoice-lines.ts`'s `getLiveInvoice()` (added 2026-09-09, extracted from `/api/quickbooks/invoice-lines`) | That route AND the assistant chat's `preview_invoice_edit` tool (`lib/invoice-edit-lookup.ts`) | Real money — a bug here means the chat's "before" side of an edit diff (and the real Billing Drafts edit panel) shows the wrong current state of a real invoice |
| `lib/playwright-tmp-cleanup.ts` | Every Playwright-browser-launching route (`lib/teamwork-agm.ts`, `lib/teamwork-nd.ts`) | Missing this on a new browser-launching route silently contributes to Vercel `/tmp` exhaustion for *every* route sharing that pool — see `docs/INVARIANTS.md` INV-CRON-004 |

---

## Cron / automation dependency map

What actually runs daily and what depends on what completing first. See
`vercel.json` for the authoritative schedule (verify there — see
`docs/INVARIANTS.md` INV-CRON-010, this table can drift from it).

```text
12:00,14:00,16:00,17:00,18:00  teamwork/sync-nd  (5 batches: teamwork_nd_1..5)
13:00        teamwork/sync              → refreshes companies, PIC, campaign recipients, contact persons
15:00,22:45,02:45  teamwork/sync-secretary
19:00        ar-reminder/generate        → depends on companies being current (runs after 13:00 sync)
19:30        quickbooks/sync
20:00        ar-reminder/sync-workflow   → depends on ar_reminder rows existing (runs after 19:00 generate)
21:00        late-filing/sync            → depends on ar_reminder + sync-workflow's date corrections (runs after 20:00)
```

Re-spaced 2026-08-31: ND batch 4 (18:00), Companies (was 18:30), and
Secretary's first run (was 18:45) had all drifted into the same nominal
Hobby-tier jitter hour (18) — a real, confirmed collision (see
`docs/INVARIANTS.md` INV-CRON-013). Every Playwright-launching entry above
now sits on its own distinct hour. ND was also rebalanced from 4 to 5
batches the same day — the real roster (14 people) had outgrown 4 batches'
3-worker-concurrency assumption.

The 19:00 → 20:00 → 21:00 ordering is load-bearing, not incidental — each
step reads data the previous step is expected to have already refreshed
that day. Moving one of these times without checking this chain risks a
step reading yesterday's data.
